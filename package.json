{
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "yarn --cwd packages/workspace run dev",
    "deploy": "yarn build && netlify deploy -d packages/workspace/dist --prod"
  },
  "devDependencies": {
    "@types/prettier": "^2.3.0",
    "@types/react": "^17.0.11",
    "@types/react-dom": "^17.0.8",
    "@types/styled-components": "^5.1.0",
    "lerna": "^3.20.2",
    "ts-node": "^8.10.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "peerDependencies": {
    "typescript": "*"
  }
}
